[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "mysql", "nodejs_18"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader", "npm install", "npm run build"]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=8080"
